class Deck

  CARD_NUMBERS = (2..14).to_a
  CARD_SYMBOLS = [:D, :S, :C, :H]

  def initialize
    @cards = CARD_SYMBOLS.each do |card_sym|
      CARD_NUMBERS.each do |card_num|

      end
    end
  end

end